
using System;

public class Test
{
    public static void Main(){
        int n = Convert.ToInt32(Console.ReadLine());
        int m = Convert.ToInt32(Console.ReadLine());
        int[,] array = new int[n,m];
        for(int i = 0; i<n; i++){
            for(int b = 0; b<m;b++){
                array[i,b] = Convert.ToInt32(Console.ReadLine());
            }
        }
        //Решение 
        int[,] tempArray = array;
        
        for(int i = 0; i<n; i++){
            for(int i1 = i+1; i1<n;i1++){
                bool flag = true;
                for(int b = 0; b < m; b++){
                    bool found = false;
                    for(int b1=0;b1<m;b1++){
                        if(array[i,b] == tempArray[i1,b1]){
                            found = true;
                            tempArray[i1,b1] = Int32.MinValue; // Будет работать пока не появятся элементы с минимальным значением Int32
                            break;
                        }
                    }
                    if(found == false){
                        flag = false;
                        break;
                    }
                }
                if(flag==true){
                    Console.WriteLine($"{i},{i1}");
                }
            }
        }
    }
}
